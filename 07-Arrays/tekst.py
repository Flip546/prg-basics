def number(text):
    return len(text)

def ord(array):
    ar
    smallest = array[0]
    for i in array:
        if i < smallest:
            array.insert(0, i)
        i += 1
    return array
def alph(array):
    array.sort()
    return array


